user:
  props:
    userId: String?
      key: 'user_id'
    value: String?
      key: 'value'
  use_copy_with: true
  copy_with_null: true
  equatable: true

account:
  props:
    userId: int,
      key: 'user_id'
      default: 0
