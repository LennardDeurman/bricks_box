name: feature
description: Creates a feature with default structure (view, cubit)

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.26 <0.1.0"


vars:
  name:
    type: string
    description: Cubit name
    prompt: What will be the cubit name?
