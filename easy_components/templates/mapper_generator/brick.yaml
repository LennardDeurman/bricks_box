name: mapper_generator
description: Generate the mapper runner for the body

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.26 <0.1.0"


vars:
  imports:
    type: string
  code_lines:
    type: string
  classes:
    type: string
